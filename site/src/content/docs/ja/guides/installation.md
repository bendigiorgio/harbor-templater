---
title: インストール
description: Harbor Templater CLI をインストールします。
---

## インストール

```sh
npm install -g harbor-templater
```

## 動作確認

```sh
harbor-templater --help
```

グローバルにインストールしたのにコマンドが見つからない場合は、npm のグローバル bin ディレクトリが `PATH` に含まれているか確認してください。

## Node.js 対応

このプロジェクトは Node.js 18+ を対象にしています。
